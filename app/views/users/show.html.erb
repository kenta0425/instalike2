<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @user.name %>
      </h1>
      <%= @user.profile %>
      <% if user_signed_in? %>
        <%= link_to "Profile Settings", '#' %>
      <% else %>
      <% end %>
    </section>
  </aside>
</div>